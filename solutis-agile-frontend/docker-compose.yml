version: '3.1'
services:
  agile-front:
    build:
      context: .
    ports:
      - '3000:80'
    restart: always
    env_file:
      - ./.env
    networks:
      - agile-network

networks:
  agile-network:
    driver: bridge
