/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { Client, RequestConfig, ResponseErrorConfig } from '@/api/client'
import fetch from '@/api/client'

import type { ListStepsQueryResponse } from '../types/ListSteps.ts'

function getListStepsUrl() {
  const res = { method: 'GET', url: `/api/v1/approval/steps/` as const }
  return res
}

/**
 * @description Return all configured approval steps.
 * @summary List Steps
 * {@link /api/v1/approval/steps/}
 */
export async function listSteps(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    ListStepsQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({ method: 'GET', url: getListStepsUrl().url.toString(), ...requestConfig })
  return res.data
}
